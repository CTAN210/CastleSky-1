using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

/*public class CharacterSelector : MonoBehaviour
{
    static string characterSelected;
    public Sprite chosenImage;
    public GameObject ParentPanel;
    public Animator animator;
    public List<GameObject> otherAnimators;
    public Sprite newSprite;
    public Image m_image;

    public void selectCharacter(Button button) 
    {
        characterSelected = button.name;
        Debug.Log(characterSelected);


        m_image = GetComponent<chosenImage>();
        m_image.sprite = newSprite;



        //animator.SetBool("IsOpen", true);
        //for (int i = 0; i < otherAnimators.Count; i++)
        //{
        //    otherAnimators[i].GetComponent<Animator>().SetBool("IsOpen", false);
        //}
    }

    public void updateCharacterSelected() {

        GameObject newObject = new GameObject("Chosen Character");

        Image image = newObject.AddComponent<Image>();

        image.sprite = chosenImage;

        //newObject.GetComponent<RectTransform>().SetParent(ParentPanel.transform);

        //newObject.transform.localScale += new Vector3(-0.001f, -0.001f, -0.01f);
       
       

    }
}
*/
